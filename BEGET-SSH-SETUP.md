# 🔐 Настройка SSH для автоматического деплоя на Beget

Эта инструкция поможет настроить автоматический деплой на Beget через SSH с использованием GitHub Actions.

## Шаг 1: Включение SSH на Beget

1. Войдите в **панель управления Beget**
2. Перейдите в раздел **"SSH/SFTP"** или **"Доступ"**
3. **Включите SSH доступ** (если еще не включен)
4. Запомните данные для подключения:
   - **Хост**: обычно `ssh.beget.com` или IP вашего сервера
   - **Порт**: обычно `22`
   - **Логин**: ваш логин на Beget (например, `username`)

## Шаг 2: Создание SSH ключа

На вашем локальном компьютере выполните:

```bash
# Создаем новый SSH ключ для деплоя (без пароля)
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/beget_deploy

# Будет создано 2 файла:
# ~/.ssh/beget_deploy      - приватный ключ (СЕКРЕТНЫЙ!)
# ~/.ssh/beget_deploy.pub  - публичный ключ
```

**Важно**: Когда спросит пароль (passphrase) - **оставьте пустым** (просто нажмите Enter).

## Шаг 3: Добавление публичного ключа на Beget

### Вариант A: Через веб-интерфейс Beget

1. Скопируйте **публичный** ключ:
   ```bash
   cat ~/.ssh/beget_deploy.pub
   ```
   
2. В панели Beget найдите раздел **"SSH ключи"** или **"Авторизованные ключи"**
3. Вставьте содержимое публичного ключа
4. Сохраните

### Вариант B: Через SSH (если уже есть доступ)

```bash
# Подключитесь к Beget
ssh your_login@ssh.beget.com

# Создайте папку для ключей (если нет)
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ
echo "СОДЕРЖИМОЕ_ПУБЛИЧНОГО_КЛЮЧА" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

# Выйдите
exit
```

## Шаг 4: Проверка SSH подключения

Проверьте, что можете подключиться с использованием ключа:

```bash
ssh -i ~/.ssh/beget_deploy your_login@ssh.beget.com
```

Если подключение успешно - отлично! Если нет - проверьте шаги выше.

## Шаг 5: Узнайте путь к папке сайта на Beget

После подключения по SSH выполните:

```bash
# Узнайте домашнюю директорию
pwd
# Обычно: /home/your_login

# Перейдите в папку сайта
cd your-domain.ru/public_html
# или просто
cd public_html

# Узнайте полный путь
pwd
# Запомните этот путь, например: /home/your_login/your-domain.ru/public_html
```

## Шаг 6: Настройка GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"** и добавьте следующие секреты:

### SSH_PRIVATE_KEY
Скопируйте **приватный** ключ:
```bash
cat ~/.ssh/beget_deploy
```
Вставьте **полностью весь вывод** (включая строки `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)

### REMOTE_HOST
Хост Beget (обычно `ssh.beget.com` или IP адрес)

### REMOTE_USER
Ваш логин на Beget (например, `username`)

### REMOTE_PORT
Порт SSH (обычно `22`)

### REMOTE_PATH
Полный путь к папке сайта (из Шага 5), например:
```
/home/username/your-domain.ru/public_html
```

## Пример GitHub Secrets:

```
SSH_PRIVATE_KEY = -----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
...
-----END OPENSSH PRIVATE KEY-----

REMOTE_HOST = ssh.beget.com
REMOTE_USER = mylogin
REMOTE_PORT = 22
REMOTE_PATH = /home/mylogin/my-domain.ru/public_html
```

## Шаг 7: Тестирование автоматического деплоя

1. Сделайте любое изменение в коде
2. Закоммитьте и запушьте в `main` ветку:
   ```bash
   git add .
   git commit -m "Test auto-deploy"
   git push origin main
   ```

3. Откройте **Actions** в вашем репозитории на GitHub
4. Наблюдайте за процессом деплоя
5. После завершения проверьте ваш сайт на Beget

## 🎉 Готово!

Теперь при каждом push в ветку `main`:
1. GitHub Actions автоматически соберет проект
2. Загрузит собранные файлы на Beget через SSH
3. Ваш сайт автоматически обновится!

## Устранение проблем

### Ошибка: "Permission denied (publickey)"

**Решение:**
1. Проверьте, что публичный ключ добавлен на Beget
2. Убедитесь, что приватный ключ в GitHub Secrets полный (включая BEGIN/END строки)
3. Проверьте права на файлы:
   ```bash
   chmod 700 ~/.ssh
   chmod 600 ~/.ssh/authorized_keys
   ```

### Ошибка: "No such file or directory"

**Решение:**
1. Проверьте правильность пути в `REMOTE_PATH`
2. Убедитесь, что папка существует на сервере
3. Создайте папку вручную через SSH

### Файлы не обновляются

**Решение:**
1. Проверьте логи в GitHub Actions
2. Убедитесь, что `SOURCE: "out/"` в workflow файле правильный
3. Проверьте, что на Beget достаточно места

## Дополнительные настройки

### Деплой только определенных файлов

Измените `.github/workflows/deploy-beget.yml`:

```yaml
EXCLUDE: "/node_modules/, /.git/, /.github/, /.DS_Store, /README.md"
```

### Деплой на другой сервер (не Beget)

Просто измените `REMOTE_HOST`, `REMOTE_USER` и `REMOTE_PATH` на соответствующие значения вашего сервера.

### Ручной запуск деплоя

В репозитории на GitHub:
1. Перейдите в **Actions**
2. Выберите **"Deploy to Beget via SSH"**
3. Нажмите **"Run workflow"**

## Безопасность

⚠️ **Важно:**
- **НИКОГДА** не коммитьте приватный ключ в репозиторий
- Используйте GitHub Secrets для всех конфиденциальных данных
- Регулярно обновляйте SSH ключи
- Используйте разные ключи для разных проектов

## Альтернативные методы

Если SSH недоступен на Beget, используйте:
- **FTP деплой** (см. DEPLOYMENT-BEGET.md)
- **Ручную загрузку** через файловый менеджер
- **Git pull** на сервере (если есть доступ к Git)

---

**Нужна помощь?** Проверьте логи в GitHub Actions или обратитесь в поддержку Beget.
