# 💰 Система управления ценами

## Обзор

Цены на услуги загружаются из статического JSON файла `public/prices/prices.json`.

## 🔗 Ссылки

- **Файл с ценами**: `public/prices/prices.json`
- **URL на сайте**: `/prices/prices.json`

## 📋 Структура JSON файла

Файл содержит объект с категориями услуг:
- **Маникюр**
- **Педикюр**
- **Услуги бровиста**
- **Подология**

### Поля для каждой услуги:

| Поле | Описание | Пример |
|---------|----------|--------|
| `service` | Название услуги | "Маникюр женский" |
| `price` | Цена | "1300" |
| `prefix` | Префикс цены (необязательно) | "от " или "+" |
| `additionalInfo` | Дополнительная информация (необязательно) | "Скидка 10% до конца месяца" |

## ✏️ Как обновить цены

1. Откройте файл `public/prices/prices.json`
2. Отредактируйте данные:
   - Измените цену
   - Добавьте дополнительную информацию в `additionalInfo`
   - Добавьте новую услугу (новый объект в массиве категории)
3. Сохраните файл
4. Выполните `npm run build` и задеплойте на сервер

## 🛠️ Архитектура

```
public/prices/prices.json (Статический файл)
    ↓
components/Prices/Prices.tsx (React Component загружает через fetch)
    ↓
Отображение на сайте
```

## 🚀 Деплой на Vercel

Просто задеплойте проект - файл `public/prices/prices.json` автоматически будет доступен по URL `/prices/prices.json`.

## 🧪 Тестирование

### Локально

```bash
# Установить зависимости
npm install

# Запустить dev сервер
npm run dev

# Проверить JSON файл
curl http://localhost:3000/prices/prices.json

# Открыть в браузере
http://localhost:3000
```

### На продакшене

```bash
# Проверить JSON файл
curl https://your-domain.vercel.app/prices/prices.json
```

## 📝 Примеры использования additionalInfo

Вы можете добавлять полезную информацию в колонку `additionalInfo`:

- "Акция: скидка 20% до конца месяца"
- "Для постоянных клиентов"
- "Длительность процедуры: 1.5 часа"
- "Требуется предварительная запись"
- "Новинка!"

Эта информация будет отображаться под названием услуги мелким текстом.

## 🔧 Разработка

### Добавление новой категории

1. Откройте файл `public/prices/prices.json`
2. Добавьте новый ключ с названием категории
3. Заполните массив услуг для этой категории
4. Сохраните файл и пересоберите проект

Пример:
```json
{
  "Маникюр": [...],
  "Педикюр": [...],
  "Новая категория": [
    {
      "service": "Название услуги",
      "price": "1000",
      "prefix": "от ",
      "additionalInfo": "Дополнительная информация"
    }
  ]
}
```

### Изменение структуры данных

Если нужно изменить структуру данных, обновите:
1. `types/prices.ts` - TypeScript типы
2. `public/prices/prices.json` - файл с данными
3. `components/Prices/Prices.tsx` - отображение (если требуется)
