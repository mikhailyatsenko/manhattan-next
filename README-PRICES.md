# 💰 Система управления ценами через Google Sheets

## Обзор

Цены на услуги загружаются из Google Таблицы в реальном времени, что позволяет обновлять их без изменения кода.

## 🔗 Ссылки

- **Google Таблица с ценами**: [Открыть таблицу](https://docs.google.com/spreadsheets/d/14StsbfQBd_b1Hk75bbRpzpfJ6lvvWdVkUz3_tO3xuYE/edit)
- **API Endpoint**: `/api/prices`
- **Service Account Email**: `sheets-price@manhattan-487210.iam.gserviceaccount.com`

## 📋 Структура таблицы

Каждая категория услуг находится на отдельном листе:
- **Маникюр**
- **Педикюр**
- **Услуги бровиста**
- **Подология**

### Колонки в каждом листе:

| Колонка | Описание | Пример |
|---------|----------|--------|
| `service` | Название услуги | "Маникюр женский" |
| `price` | Цена | "1300" или "1300" |
| `prefix` | Префикс цены (необязательно) | "от " или "+" |
| `additionalInfo` | Дополнительная информация | "Скидка 10% до конца месяца" |

## ✏️ Как обновить цены

1. Откройте [Google Таблицу](https://docs.google.com/spreadsheets/d/14StsbfQBd_b1Hk75bbRpzpfJ6lvvWdVkUz3_tO3xuYE/edit)
2. Выберите нужный лист (категорию)
3. Отредактируйте данные:
   - Измените цену
   - Добавьте дополнительную информацию в `additionalInfo`
   - Добавьте новую услугу (новую строку)
4. Сохраните (автоматически)
5. **Подождите до 5 минут** - данные обновятся на сайте

## 🔄 Кеширование

- Данные кешируются на **5 минут** (300 секунд)
- После изменений в Google Sheets, они появятся на сайте в течение 5 минут
- В dev-режиме можно отключить кеш: `export const revalidate = 0` в `app/api/prices/route.ts`

## 🛠️ Архитектура

```
Google Sheets
    ↓
lib/googleSheets.ts (Google Sheets API)
    ↓
app/api/prices/route.ts (API Route)
    ↓
components/Prices/Prices.tsx (React Component)
    ↓
Отображение на сайте
```

### Fallback механизм

Если Google Sheets недоступен:
1. API пытается получить данные из Google Sheets
2. В случае ошибки используются локальные данные из `prices_json/prices.json`
3. Пользователь видит предупреждение: "Не удалось загрузить цены. Показываем локальные данные."

## 🔐 Безопасность

- **Vercel (production):** Service Account credentials хранятся в переменной окружения `GOOGLE_SHEETS_CREDENTIALS`
- **Локально:** используется файл `manhattan-487210-5ef58e0a375e.json` (добавлен в `.gitignore`)
- Файл с credentials **не попадает** в Git репозиторий
- Таблица доступна только Service Account с email `sheets-price@manhattan-487210.iam.gserviceaccount.com`

## 📦 Необходимые пакеты

```json
{
  "googleapis": "^140.0.0"
}
```

## 🚀 Деплой на Vercel

См. подробную инструкцию в файле [DEPLOYMENT.md](./DEPLOYMENT.md)

Кратко:
1. Добавить переменную окружения `GOOGLE_SHEETS_CREDENTIALS` в Vercel
2. Задеплоить проект
3. Готово!

## 🧪 Тестирование

### Локально

```bash
# Установить зависимости
npm install

# Запустить dev сервер (credentials берутся из manhattan-487210-5ef58e0a375e.json)
npm run dev

# Проверить API
curl http://localhost:3000/api/prices

# Открыть в браузере
http://localhost:3000
```

### На продакшене

```bash
# Проверить API
curl https://your-domain.vercel.app/api/prices
```

## 📝 Примеры использования additionalInfo

Вы можете добавлять полезную информацию в колонку `additionalInfo`:

- "Акция: скидка 20% до конца месяца"
- "Для постоянных клиентов"
- "Длительность процедуры: 1.5 часа"
- "Требуется предварительная запись"
- "Новинка!"

Эта информация будет отображаться под названием услуги мелким текстом.

## 🔧 Разработка

### Добавление новой категории

1. Создайте новый лист в Google Таблице
2. Добавьте заголовки: `service`, `price`, `prefix`, `additionalInfo`
3. Заполните данные
4. Категория автоматически появится на сайте

### Изменение структуры данных

Если нужно изменить структуру данных, обновите:
1. `types/prices.ts` - TypeScript типы
2. `lib/googleSheets.ts` - логику получения данных
3. `components/Prices/Prices.tsx` - отображение

## 📞 Поддержка

При проблемах проверьте:
1. Service Account имеет доступ к таблице (Editor)
2. Google Sheets API включен в Google Cloud Console
3. Переменная `GOOGLE_SHEETS_CREDENTIALS` правильно настроена на Vercel
4. Кеш (подождите 5 минут после изменений)
