# Деплой на Beget (статический хостинг)

## Подготовка проекта

Проект настроен для статической сборки (Static Site Generation). Все данные о ценах загружаются из локального JSON файла `prices_json/prices.json`.

## Шаги для деплоя на Beget

### 1. Сборка проекта

Выполните команду для создания статической сборки:

```bash
npm install
npm run build
```

После сборки в папке `out/` будут созданы статические HTML, CSS, JS файлы.

### 2. Структура папки `out/`

После команды `npm run build` создается папка `out/` со следующей структурой:

```
out/
├── index.html          # Главная страница
├── _next/              # JavaScript и CSS файлы Next.js
├── favicon/            # Иконки
├── img/                # Изображения
└── ...                 # Другие статические файлы
```

### 3. Загрузка на Beget

#### Через файловый менеджер Beget:

1. Войдите в панель управления Beget
2. Откройте **Файловый менеджер**
3. Перейдите в папку вашего сайта (обычно `public_html` или папка домена)
4. **Удалите все содержимое** папки сайта (если там есть старые файлы)
5. **Загрузите всё содержимое папки `out/`** (не саму папку, а её содержимое)

#### Через FTP:

1. Подключитесь к Beget через FTP клиент (FileZilla, Total Commander и т.д.)
   - Хост: ваш домен или IP
   - Логин и пароль: из панели Beget
2. Перейдите в папку сайта
3. Удалите старые файлы
4. Загрузите содержимое папки `out/`

### 4. Настройка .htaccess (важно для Next.js SPA)

Создайте файл `.htaccess` в корне сайта на Beget со следующим содержимым:

```apache
# Включить Rewrite Engine
RewriteEngine On

# Если файл существует - показать его
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Все остальные запросы перенаправлять на index.html
RewriteRule ^ index.html [L]

# Кеширование статических файлов
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  ExpiresByType application/x-javascript "access plus 1 month"
  ExpiresByType text/javascript "access plus 1 month"
</IfModule>

# GZIP сжатие
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE text/javascript
  AddOutputFilterByType DEFLATE application/javascript
  AddOutputFilterByType DEFLATE application/x-javascript
</IfModule>
```

### 5. Проверка

После загрузки откройте ваш сайт в браузере и проверьте:
- ✅ Главная страница открывается
- ✅ Все изображения загружаются
- ✅ Раздел с ценами отображается корректно
- ✅ Навигация работает

## Обновление цен

Чтобы обновить цены на сайте:

1. Отредактируйте файл `prices_json/prices.json`
2. Выполните сборку: `npm run build`
3. Загрузите обновленную папку `out/` на Beget

## Автоматизация (опционально)

Вы можете автоматизировать деплой с помощью:

### GitHub Actions через SSH (рекомендуется) ⭐

**Преимущества SSH:**
- ✅ Безопаснее, чем FTP
- ✅ Быстрее загрузка файлов
- ✅ Автоматическое удаление старых файлов
- ✅ Поддержка rsync для инкрементальной загрузки

Проект уже настроен! Файл `.github/workflows/deploy-beget.yml` создан.

**Шаги настройки:**

1. **Создайте SSH ключи** (подробно в `BEGET-SSH-SETUP.md`):
   ```bash
   ssh-keygen -t ed25519 -C "github-actions-beget" -f ~/.ssh/beget_deploy
   ```

2. **Добавьте публичный ключ на Beget**:
   - Зайдите в панель Beget → SSH/SFTP
   - Добавьте содержимое `~/.ssh/beget_deploy.pub`

3. **Добавьте секреты в GitHub** (Settings → Secrets and variables → Actions):
   - `BEGET_SSH_HOST` - например: `yoursite.beget.tech`
   - `BEGET_SSH_USER` - ваш логин на Beget
   - `BEGET_SSH_KEY` - содержимое приватного ключа `~/.ssh/beget_deploy`
   - `BEGET_DEPLOY_PATH` - путь к сайту, например: `/home/u123456/yoursite.ru/public_html`

4. **Пушьте в main ветку** - деплой запустится автоматически!

📖 **Полная инструкция**: см. файл `BEGET-SSH-SETUP.md`

### GitHub Actions через FTP (альтернатива)

Создайте файл `.github/workflows/deploy-ftp.yml`:

```yaml
name: Deploy to Beget (FTP)

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build
        run: npm run build
        
      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./out/
          server-dir: /public_html/
```

Добавьте секреты в GitHub (Settings → Secrets):
- `FTP_SERVER` - адрес FTP сервера Beget
- `FTP_USERNAME` - логин FTP
- `FTP_PASSWORD` - пароль FTP

## Отличия от Vercel версии

| Функция | Vercel | Beget (статический) |
|---------|--------|---------------------|
| **Цены** | Загружаются из Google Sheets через API | Загружаются из `prices.json` |
| **Обновление цен** | Автоматически каждые 5 минут | Требует пересборки и загрузки |
| **API Routes** | Поддерживаются | Не поддерживаются |
| **Оптимизация изображений** | Автоматическая | Отключена |
| **Стоимость** | Бесплатно для малых проектов | Зависит от тарифа Beget |

## Преимущества статической сборки

✅ Быстрая загрузка страниц  
✅ Работает на любом хостинге  
✅ Не требует Node.js на сервере  
✅ Высокая безопасность  
✅ Низкая стоимость хостинга  

## Недостатки

❌ Цены не обновляются автоматически из Google Sheets  
❌ Требуется пересборка для любых изменений  
❌ Нет серверных функций (API routes)  

## Рекомендации

- **Для часто обновляемых цен** → используйте Vercel (DEPLOYMENT.md)
- **Для редко обновляемых цен** → используйте Beget (эта инструкция)
- **Для максимальной производительности** → используйте CDN (Cloudflare) поверх Beget

## Поддержка

Если возникли проблемы:
1. Проверьте, что все файлы из `out/` загружены корректно
2. Проверьте наличие `.htaccess` файла
3. Проверьте права доступа к файлам (должны быть 644)
4. Очистите кеш браузера (Ctrl+F5)

## Команды для локальной разработки

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Просмотр собранной версии локально
npx serve out
```
