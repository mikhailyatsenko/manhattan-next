# 🚀 Деплой Next.js приложения на Beget

## Подготовка проекта

Проект настроен для статического экспорта на обычный хостинг (Apache).

## 📦 Шаг 1: Сборка проекта

Выполните сборку проекта локально:

```bash
# Установите зависимости (если еще не установлены)
npm install

# Выполните сборку для экспорта
npm run build
```

После успешной сборки будет создана папка **`out/`** со статическими файлами.

## 📁 Шаг 2: Что загружать на Beget

**Загрузите ВСЁ содержимое папки `out/` на ваш хостинг Beget.**

### Структура папки `out/`:

```
out/
├── index.html           # Главная страница
├── 404.html            # Страница ошибки 404
├── .htaccess           # Конфигурация Apache (ВАЖНО!)
├── _next/              # CSS, JS и другие ресурсы Next.js
├── favicon/            # Иконки сайта
├── img/                # Изображения
├── prices/             # JSON с ценами
│   └── prices.json
└── manifest.json       # PWA манифест
```

## 🌐 Шаг 3: Загрузка на Beget

### Вариант А: Через FTP/SFTP (FileZilla, WinSCP)

1. Подключитесь к вашему хостингу через FTP/SFTP
2. Перейдите в корневую папку сайта (обычно `public_html/` или `www/`)
3. **Загрузите ВСЁ содержимое папки `out/`** в корневую папку сайта
4. Убедитесь, что файл `.htaccess` загружен (он скрытый!)

### Вариант Б: Через файловый менеджер Beget

1. Войдите в панель управления Beget
2. Откройте "Файловый менеджер"
3. Перейдите в папку `public_html/` (или папку вашего сайта)
4. Загрузите все файлы из папки `out/`
5. Проверьте наличие файла `.htaccess`

## ⚙️ Шаг 4: Проверка .htaccess

Файл `.htaccess` уже находится в папке `out/` и настроен для работы на Apache.

**Важно:** Убедитесь, что `.htaccess` загружен! Он скрытый файл и может не отображаться в некоторых FTP-клиентах. Включите отображение скрытых файлов.

### Что делает .htaccess:

- ✅ Настраивает URL routing для Next.js
- ✅ Включает сжатие (gzip)
- ✅ Настраивает кеширование статики
- ✅ Добавляет заголовки безопасности
- ✅ Опционально: редирект на HTTPS и www

### Настройка редиректов (опционально)

Откройте файл `out/.htaccess` и раскомментируйте нужные строки:

**Для редиректа на HTTPS:**
```apache
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
```

**Для редиректа на www (или наоборот):**
```apache
RewriteCond %{HTTP_HOST} ^yourdomain\.ru$ [NC]
RewriteRule ^(.*)$ https://www.yourdomain.ru/$1 [R=301,L]
```

Замените `yourdomain.ru` на ваш домен.

## 🔄 Обновление сайта

При каждом изменении:

1. Внесите изменения в код
2. Выполните `npm run build`
3. Загрузите **обновленное содержимое** папки `out/` на Beget (перезапишите старые файлы)

## 📝 Обновление цен

Чтобы обновить только цены **БЕЗ** пересборки всего проекта:

1. Отредактируйте файл `public/prices/prices.json`
2. Выполните `npm run build`
3. Загрузите на хостинг **только** файл `out/prices/prices.json`

Или ещё проще:

1. Отредактируйте файл прямо на хостинге через файловый менеджер Beget:
   - Путь: `public_html/prices/prices.json`
2. Сохраните изменения

## ✅ Проверка работы

После загрузки проверьте:

1. **Главная страница:** `https://your-domain.ru/`
2. **JSON с ценами:** `https://your-domain.ru/prices/prices.json`
3. **Страница 404:** `https://your-domain.ru/nesushchestvuyushchaya-stranica/`

## 🐛 Возможные проблемы

### Проблема: Страницы не открываются (404 ошибка)

**Решение:**
- Убедитесь, что файл `.htaccess` загружен
- Проверьте, включен ли `mod_rewrite` на Beget (обычно включен по умолчанию)
- Свяжитесь с поддержкой Beget для включения `mod_rewrite`

### Проблема: Стили не загружаются

**Решение:**
- Проверьте, что папка `_next/` загружена полностью
- Очистите кеш браузера (Ctrl+F5)

### Проблема: Изображения не отображаются

**Решение:**
- Убедитесь, что папки `img/` и `favicon/` загружены
- Проверьте права доступа к файлам (должно быть 644 для файлов, 755 для папок)

### Проблема: Цены не загружаются

**Решение:**
- Проверьте наличие файла `out/prices/prices.json`
- Откройте в браузере: `https://your-domain.ru/prices/prices.json`
- Проверьте, что JSON валидный

## 📊 Размер проекта

Размер папки `out/` обычно составляет 2-5 MB (зависит от количества изображений).

## 🔒 Безопасность

- `.htaccess` настроен с базовыми заголовками безопасности
- Кеширование настроено для оптимальной производительности
- Статические файлы кешируются на 1 год
- HTML и JSON не кешируются (всегда актуальные данные)

## 💡 Советы

1. **Всегда делайте backup** перед загрузкой новой версии
2. **Используйте SFTP** вместо FTP для безопасности
3. **Проверяйте сайт** после каждого обновления
4. **Очищайте кеш браузера** при тестировании изменений
5. **Используйте .zip архив** для быстрой загрузки через панель Beget

### Загрузка через ZIP

1. Создайте архив папки `out/`:
   ```bash
   cd out
   zip -r ../site.zip .
   ```
2. Загрузите `site.zip` через файловый менеджер Beget
3. Распакуйте архив в папку `public_html/`

## 📞 Поддержка Beget

Если возникли проблемы с настройкой Apache или `.htaccess`:
- Техподдержка Beget: https://beget.com/ru/support
- Документация: https://beget.com/ru/kb

## 🎉 Готово!

После загрузки ваш сайт будет доступен по адресу вашего домена!
