# 🚀 Быстрый старт: Деплой на Beget

## Шаг 1: Сборка проекта

```bash
npm install
npm run build
```

После этого в папке `out/` будет готовый статический сайт (3.2 MB, 114 файлов).

## Шаг 2: Загрузка на Beget

### Вариант A: Через FTP
1. Подключитесь к Beget через FTP (FileZilla, Total Commander)
2. Откройте папку вашего домена (обычно `public_html` или `your-domain.ru`)
3. **Загрузите ВСЁ содержимое папки `out/`** (включая `.htaccess`)

### Вариант B: Через файловый менеджер Beget
1. Войдите в панель Beget → Файловый менеджер
2. Перейдите в папку домена
3. Загрузите все файлы из `out/`

## ✅ Готово!

Откройте ваш домен в браузере - сайт должен работать!

## 📝 Важные файлы в `out/`:

- `index.html` - главная страница (32 KB)
- `.htaccess` - настройки сервера (уже включен)
- `_next/` - JavaScript и CSS
- `img/` - картинки
- `favicon/` - иконки

## 🔄 Обновление цен

Чтобы изменить цены:

1. Отредактируйте `prices_json/prices.json`
2. Выполните: `npm run build`
3. Загрузите обновленные файлы из `out/` на Beget

## 📚 Полная документация

См. файл `DEPLOYMENT-BEGET.md` для:
- Автоматизации через GitHub Actions
- Настройки кеширования
- Решения проблем
- И многого другого

---

**Размер сайта:** 3.2 MB  
**Файлов:** 114  
**Время загрузки на Beget:** ~2-5 минут (зависит от скорости интернета)
